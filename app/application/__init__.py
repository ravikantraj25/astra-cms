"""Application layer — use cases and application services.

Orchestrates domain objects to fulfill user intents. Depends only on the
domain layer; infrastructure is injected via ports (abstract interfaces).
"""
